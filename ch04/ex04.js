// 예제를 직접 쳐보면서 이해하기
var userId = 1;
var userName = "Lee";

var user = { id: 1, name: 'Lee' };

var users = [
    { id: 1, name: 'Lee'},
    { id: 2, name: 'Kim'}
];

var result = 10 + 20; // 30

// 값의 할당
console.log(score); // undefined

score = 80; // 값의 할당
var score; // 변수 선언

console.log(score); 